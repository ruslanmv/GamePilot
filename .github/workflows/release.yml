# .github/workflows/release.yml
# Build distributions on GitHub Release and publish to PyPI (Trusted Publishers)
# Automatically patches version from the release tag before building.
#
# IMPORTANT: This workflow uses PyPI Trusted Publishing. For it to succeed,
# a Trusted Publisher must be configured on PyPI with these EXACT values:
#   Owner:             ruslanmv
#   Repository name:   GamePilot
#   Workflow filename: release.yml
#   Environment name:  pypi
# For the very first release, use a "Pending Publisher" on your PyPI account
# page (https://pypi.org/manage/account/publishing/). See docs/PUBLISHING.md.

name: Build & Publish to PyPI

on:
  release:
    types: [published]

permissions:
  contents: write   # upload assets to GitHub release
  id-token: write   # required for PyPI Trusted Publishing (OIDC)

jobs:
  build:
    name: Build distributions
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Extract version from release tag
        id: version
        run: |
          RAW="${GITHUB_REF#refs/tags/}"
          SEMVER="${RAW#v}"
          echo "VERSION=$SEMVER" >> "$GITHUB_ENV"
          echo "📦 Building version: $SEMVER"

      - name: Patch pyproject.toml version
        run: |
          sed -i "s/^version = \".*\"/version = \"$VERSION\"/" pyproject.toml
          echo "✅ pyproject.toml → $VERSION"
          grep '^version' pyproject.toml

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install build backend
        run: |
          python -m pip install --upgrade pip build

      - name: Build wheel and sdist
        run: |
          # Just in case, ensure clean dist/ in the CI workspace
          rm -rf dist build
          python -m build

      - name: Upload artefacts to the GitHub release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/*.whl
            dist/*.tar.gz

      - name: Persist artefacts for publish job
        uses: actions/upload-artifact@v4
        with:
          name: python-dists
          path: dist/

  publish:
    name: Publish to PyPI
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: pypi
      url: https://pypi.org/project/gamepilot/
    steps:
      - name: Download artefacts
        uses: actions/download-artifact@v4
        with:
          name: python-dists
          path: dist/

      - name: Publish via Trusted Publishing
        uses: pypa/gh-action-pypi-publish@release/v1
        # no extra config needed; it will upload dist/* using OIDC